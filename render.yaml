services:
  - type: web
    name: kali-academy
    runtime: node
    plan: free
    buildCommand: npm install pnpm --no-save && ./node_modules/.bin/pnpm install --no-frozen-lockfile && ./node_modules/.bin/pnpm run build:render
    startCommand: node artifacts/api-server/dist/index.js
    healthCheckPath: /api/healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
